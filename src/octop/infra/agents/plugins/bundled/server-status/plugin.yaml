id: server-status
version: 0.2.0
name: Server Status
description: Show the current server OS, kernel, and CPU / memory / disk load in chat
icon: "🖥️"
kind: tool
entry: main.py
requires:
  - psutil>=5.9
ui:
  entry: ui/index.js
  manifest: ui/manifest.json
